<% for column in Hits.content_columns %>
<p>
  <b><%= column.human_name %>:</b> <%=h @hits.send(column.name) %>
</p>
<% end %>

<%= link_to 'Edit', :action => 'edit', :id => @hits %> |
<%= link_to 'Back', :action => 'list' %>
